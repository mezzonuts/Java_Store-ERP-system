spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/sosha
    username: sosha
    password: sosha
    hikari:
      maximum-pool-size: 20
  jpa:
    properties:
      hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect
  flyway:
    locations: classpath:db/migration/common,classpath:db/migration/postgres
